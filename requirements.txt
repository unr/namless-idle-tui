# Core dependencies for TUI version
textual>=0.43.0,<0.44.0
aiosqlite>=0.19.0
python-dateutil>=2.8.2
pydantic>=2.0.0

# Web version dependencies
# Note: textual-serve requires textual>=6.0.0, which conflicts with our TUI version
# For web deployment, use a separate virtual environment with:
# pip install textual-serve (will install textual 6.4.0+)
# textual-web>=0.8.0  # For cloud deployment (optional)

# Dev dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.7.0

# Optional formatting/linting
autopep8>=2.0.0
isort>=5.12.0