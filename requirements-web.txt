# Dependencies for Web version
# Note: This requires a separate virtual environment from the TUI version
# due to conflicting Textual versions

# Web server dependencies
textual-serve>=1.1.2  # This will install textual 6.4.0+
aiohttp>=3.9.5
aiohttp-jinja2>=1.6

# Core app dependencies (same as TUI)
aiosqlite>=0.19.0
python-dateutil>=2.8.2
pydantic>=2.0.0

# Optional: For cloud deployment
# textual-web>=0.8.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0